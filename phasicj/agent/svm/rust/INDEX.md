# PhasicJ Agent SVM Rust Bindings

This package includes Rust bindings for the Agent's SVM library. With these,
Rust code can call the SVM library (which was generated from Java via GraalVM
`native-image`).
