lower_latin_alpha = _{ 'a'..'z' }

snake_case_name = _{ (lower_latin_alpha | "_")+ }

conf_flag_name = @{ snake_case_name }

agent_option = @{ conf_flag_name }

agent_options_list = { SOI ~ (agent_option ~ ("," ~ agent_option)*)? ~ EOI }

WHITESPACE = _{ " " }
