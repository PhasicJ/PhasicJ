# Renaissance Benchmarking Suite Prototype

This prototype is designed to demonstrate how to run the Renaissance benchmark
suite such that it is invoked by `bazel run` and with a simple Java agent
instrumenting it.
